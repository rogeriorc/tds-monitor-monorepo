{
  "name": "@totvs/tds-monitor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build --stream --concurrency=1",
    "watch": "lerna run --parallel build --scope @totvs/tds-languageclient --scope @totvs/tds-monitor-frontend -- -- --watch",
    "start": "lerna run start --scope @totvs/tds-monitor-standalone",
    "clean": "lerna run clean --stream"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.16.3"
  }
}
